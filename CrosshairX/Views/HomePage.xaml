<Page
    x:Class="CrosshairX.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:CrosshairX.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource PrimaryBackground}">

    <Page.DataContext>
        <viewModels:HomeViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <Grid>
        <StackPanel>
            <TextBlock Text="Home page" />
            <!--<Button Content="Set" Command="{x:Bind ViewModel.SwitchLanguageCommand}" />-->
            <!--<Button Content="Lang" Command="{x:Bind ViewModel}"></Button>-->
        </StackPanel>
    </Grid>
</Page>
